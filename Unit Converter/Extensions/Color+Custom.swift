import SwiftUI

extension Color {
    struct Custom {
        static let cardBackground = Color("cardBackground")
        static let iconBackground = Color("iconBackground")
        static let resultText = Color("resultText")
    }
} 